import React from 'react';

// This is a placeholder component to prevent build errors.
// It is not used in the application.
const PixelForestScreen: React.FC = () => {
  return (
    <div style={{ padding: '20px', textAlign: 'center' }}>
      <h1>Componente Obsoleto</h1>
      <p>Este modo de juego ha sido reemplazado.</p>
    </div>
  );
};

export default PixelForestScreen;
